# Sensitive files — do NOT commit
scripts/serviceAccountKey.json
.env
.env.local

# Node modules (if you run npm install for the seed script)
node_modules/
# package-lock.json is intentionally committed so GitHub Actions can use npm ci

# Office temporary lock files
~$*

# macOS
.DS_Store

# Editor
.vscode/
